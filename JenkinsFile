@Library('pipeline') _

timestamps {
   node {
      echo 'Hello World'

      stage('Build'){
         sh "pwd"
         sh "ls"
      }

      stage('Test'){
         JenkinsPipelineVars() 
      }
   }
}
