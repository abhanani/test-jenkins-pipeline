@Library('pipeline') _

timestamps {
   node('any') {
      stage('Build'){
         python3 build.py
      }
   
      stage('Test'){
         python3 test.py
      }
      
      stage('Promote'){
         echo 'Build promoted to functional'
      }
   }
}

