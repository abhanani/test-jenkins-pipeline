library 'pipeline'

timestamps {
   node {
      echo 'Hello World'

      stage('calling function'){
         JenkinsPipelineVars()
      }

      stage('calling function diff'){
         JenkinsPipelineVars 
      }
   }
}
